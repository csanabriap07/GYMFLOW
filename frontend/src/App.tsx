/**
 * Scaffold inicial. Las pantallas reales (kiosko / backoffice) se agregan
 * feature por feature (ver spec/features/), no aquí.
 */
export default function App() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-gray-50">
      <h1 className="text-2xl font-bold text-gray-800">
        GymFlow — scaffold listo
      </h1>
    </div>
  );
}
